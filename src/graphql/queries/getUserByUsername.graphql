query FindByUsername($username: String!) {
    getUserByUsername(username: $username) {
        id
        email
        username
        items {
            id
            imageurl
            price
        }
    }
}